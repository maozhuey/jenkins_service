# 网络配置中心化文件
# 所有脚本应该从此文件读取网络配置，确保配置一致性
# 修改时间: $(date)

# 主要网络配置
NETWORK_NAME="tbk_app-network"
SUBNET="172.21.0.0/16"
GATEWAY="172.21.0.1"

# 生产环境网络配置
PRODUCTION_NETWORK_NAME="tbk-production-network"
PRODUCTION_SUBNET="172.21.0.0/16"
PRODUCTION_GATEWAY="172.21.0.1"

# 本地开发网络配置
LOCAL_NETWORK_NAME="tbk-local-network"
LOCAL_SUBNET="172.21.0.0/16"
LOCAL_GATEWAY="172.21.0.1"

# 数据库配置
MYSQL_CONTAINER_NAME="tbk-mysql"
MYSQL_PORT="3306"

# 应用配置
APP_CONTAINER_PREFIX="tbk"
NGINX_CONTAINER_NAME="tbk-nginx"

# 配置版本信息
CONFIG_VERSION="1.0.0"
LAST_UPDATED="$(date '+%Y-%m-%d %H:%M:%S')"

# 使用说明:
# 在脚本中使用: source /path/to/network.conf
# 然后可以直接使用变量: $NETWORK_NAME, $SUBNET 等